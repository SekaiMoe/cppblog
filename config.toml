# Blog basic configuration
blog_name = "My Blog"
blog_description = "A simple markdown blog"
posts_directory = "posts"
blog_author = "SekaiMoe"

# Ports
port = 5444

# Hot reload configuration(seconds)
hot_reload = true
reload_interval = 1
